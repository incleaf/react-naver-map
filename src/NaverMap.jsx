import React, { PropTypes } from 'react'

const NaverMap = React.createClass({
    render () {
        const text = "Hello Naver Map";
        return (
            <div>
                {this.text}
            </div>
        )
    }
})

export default NaverMap
